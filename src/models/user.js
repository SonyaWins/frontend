export default class User{
    constructor(type, username, names, lastnames, mail, password) {
        this.type = type;
        this.username = username;
        this.names = names;
        this.lastnames = lastnames;
        this.mail = mail;
        this.password = password;
    }
}